class GameController:
    def __init__(self, board, agent):
        self.board = board
        self.agent = agent

    def makeMove(self, board, playerRow, playerCol, nextRow, nextCol):
        return
